# Configuración del servidor SMTP (ejemplo con Gmail)

spring.application.name=web
springdoc.swagger-ui.enabled=true
springdoc.api-docs.path=/api-docs
springdoc.swagger-ui.path=/docs/swagger-ui.html

spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=progwebufps@gmail.com
spring.mail.password=rlfqkcrbknkaoqge

# Propiedades adicionales para la configuración de correo
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Configuración del servidor
server.port=8081

# Configuración de logging (opcional, pero útil para depuración)
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# Configuración de CORS (Cross-Origin Resource Sharing)
spring.web.cors.allowed-origins=http://localhost:8080
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true